{
  "name": "stock-openui-example-ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "generate:prompt": "generate-ai-ui-prompt --library openui --out src/generated/components-prompt.txt --json-out src/generated/components.json",
    "dev": "pnpm run generate:prompt && vite",
    "build": "pnpm run generate:prompt && tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@nube/ai-ui-react": "workspace:*",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.4.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
